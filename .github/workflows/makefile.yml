name: Hello World CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-20.04
            dist_target:
          - os: ubuntu-20.04
            dist_target: win32
          - os: macos-11
            dist_target:
    runs-on: ${{ matrix.os }}
    name: "${{ matrix.os }}:${{ matrix.dist_target }}"
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
    - name: Compile it
      run: make
    - name: Run it
      run: ./Hello


